<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QQNotifier" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary brand color. (主色调) -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. (强调色) -->
        <item name="colorSecondary">@color/light_blue_A200</item>
        <item name="colorSecondaryVariant">@color/light_blue_A700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="android:windowLightStatusBar">false</item> <!-- 因为背景是深色(蓝)，所以状态栏图标要用浅色(白色) -->
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>

    </style>
</resources>