<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QQNotifier"
        tools:targetApi="31">

        <!-- 主界面Activity，必须有一个 intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 这是我们的通知监听服务 -->
        <service
            android:name=".QQNotificationListener"
            android:exported="true"
            android:label="QQ通知转发服务"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- !!! 关键检查点：SettingsActivity 的声明 !!! -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- !!! 关键检查点：EditSettingActivity 的声明 !!! -->
        <activity
            android:name=".EditSettingActivity"
            android:exported="false" />

        <!-- !!! 关键检查点：AppPickerActivity 的声明 !!! -->
        <activity
            android:name=".AppPickerActivity"
            android:exported="false" />

    </application>

</manifest>